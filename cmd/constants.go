package cmd

const (
	// Common defaults.
	defaultOutputDir = "./build"
	defaultFormat    = "text"

	// Server constants.
	defaultPort     = 8080
	shutdownTimeout = 5

	// Output format constants.
	formatJSON  = "json"
	formatTable = "table"
)
